/* eslint-disable no-lone-blocks */
{ /** 
    The error message will only appear when theres no available internet or when the network is slow.
*/}

export const Network = () => {
    return(
        <div>Check your internet and refresh ...</div>
    )
}